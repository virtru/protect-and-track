// import { Pure as Header } from './Header';
// import styles from './Header.css';

// describe('Header', () => {
//   it('renders correctly', () => {
//     const wrapper = shallow(<Header />);

//     expect(wrapper.find(`.${styles.company}`).text()).toEqual('Demo: Protect & Track');
//     expect(wrapper).toMatchElement(<Header />);
//   });
// });
